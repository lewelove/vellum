if i transition entire config to lua, what rust functions can i expose via lua api to make manifest execution fully config driven? i think of `vl.manifest.build` function() that runs `vl.manifest.print_line( key = "" )` and `vl.manifest.resolve_key({ type = "", target = "", fallback = "" })` that returns string or number etc etc. for manifest stage only. all functions return text, some by doing something external, some by utilizing built-in rust ability to read file tags. also using `vl.config()` to provide rust config struct population. and the compile time becomes strictly text based, with `update` command executing conditional fallback to triggering some heavy functions (also written in lua using `vl.exec_cmd` or built-in ones) based on tracked in lock files mtimes and bytesizes.

and the question: i want to make compiler only text driven. it means that no dynamic computation like calculating anything except file's metadata and manifest merging must occur at comptime. all data for ui is externalized in manifests. the only problem is state drift. how do we know the lyrics are out of sync? how do we know the cover was updated? it will all happen at `update` time not compile one. i think we can make few scripts that are run against the lock files. how we determine link between manifest carrying the data and actual state this data represents?
