# add fast modal drawer open

add a way to render modal drawer at the home tab instantly, with zero animations. tie it up to `open album` button in queue tab, so when pressed, the home tab is faded in view with drawer already fully open
